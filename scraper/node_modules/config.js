let secrets = require('secret')

let config = {
	dbUser: "wastee",
	...secrets
}

config.uri = `mongodb+srv://${config.dbUser}:${config.dbPwd}@cluster0.pjx4h.mongodb.net/${config.dbName}?retryWrites=true&w=majority`

module.exports = config